// 将秒数改为分钟+秒数


function secondToMinute(sec){
    var m = Math.floor(sec / 60);
    var s = Math.floor(sec%60);
    m = m<10?"0"+m:m;
    s = s<10?"0"+s:s;
    return m+":"+s;
}


module.exports = secondToMinute;